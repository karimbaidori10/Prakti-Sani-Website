<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>LSMD Dashboard</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>

<div class="layout">

    <aside class="sidebar">
        <div class="brand">
            <div class="brand-icon">âœš</div>
            <div>
                <h2>LSMD</h2>
                <p>Prakti-Sani</p>
            </div>
        </div>

        <nav class="nav">
            <a class="<%= (typeof active !== 'undefined' && active === 'dashboard') ? 'active' : '' %>" href="/dashboard">
                <span>ðŸ </span> Dashboard
            </a>

            <a class="<%= (typeof active !== 'undefined' && active === 'leaderboard') ? 'active' : '' %>" href="/leaderboard">
                <span>ðŸ†</span> Leaderboard
            </a>

            <a class="<%= (typeof active !== 'undefined' && active === 'users') ? 'active' : '' %>" href="/users">
                <span>ðŸ‘¥</span> Mitglieder
            </a>

            <a class="<%= (typeof active !== 'undefined' && active === 'termine') ? 'active' : '' %>" href="/termine">
                <span>ðŸ“…</span> Termine
            </a>

            <a class="<%= (typeof active !== 'undefined' && active === 'pruefungen') ? 'active' : '' %>" href="/pruefungen">
                <span>ðŸ“‹</span> PrÃ¼fungen
            </a>

            <a class="<%= (typeof active !== 'undefined' && active === 'dokumente') ? 'active' : '' %>" href="/dokumente">
                <span>ðŸ“</span> Dokumente
            </a>

            <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
                <a class="<%= (typeof active !== 'undefined' && active === 'admin') ? 'active' : '' %>" href="/admin">
                    <span>ðŸ›¡ï¸</span> Admin
                </a>
            <% } %>
        </nav>

        <div class="sidebar-bottom">
            <div class="user-card">
                <div class="avatar">
                    <% if (typeof user !== "undefined" && user && user.avatar) { %>
                        <img src="<%= user.avatar %>" alt="Avatar">
                    <% } else { %>
                        âœš
                    <% } %>
                </div>

                <div>
                    <strong><%= (typeof user !== "undefined" && user && user.username) ? user.username : "Leitung" %></strong>
                    <p>
                        <span class="online-dot"></span>
                        <%= (typeof user !== "undefined" && user && user.role) ? user.role : "Online" %>
                    </p>
                </div>
            </div>

            <a class="logout" href="/logout">â†ª Logout</a>
        </div>
    </aside>

    <main class="main">
        <div class="topbar">
            <button class="menu-btn">â˜°</button>

            <div class="topbar-right">
                <div class="search-box">ðŸ” Suchen...</div>

                <div class="bell">
                    ðŸ””
                    <span>3</span>
                </div>

                <div class="profile-circle">
                    <% if (typeof user !== "undefined" && user && user.avatar) { %>
                        <img src="<%= user.avatar %>" alt="Avatar">
                    <% } else { %>
                        <%= (typeof user !== "undefined" && user && user.username) ? user.username.charAt(0).toUpperCase() : "L" %>
                    <% } %>
                </div>
            </div>
        </div>

        <div class="page-content">
            <%- body %>
        </div>
    </main>

</div>

</body>
</html>
